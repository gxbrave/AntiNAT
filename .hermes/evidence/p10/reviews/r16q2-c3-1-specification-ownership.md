# Formal Review 1/4 — SPECIFICATION/OWNERSHIP

- Candidate: C3 = commit 256c91046a00c6c18a43facf1f6ff73b576f3b23 / tree 576a3d683ee78cbacba427a6055a2a925a2d8725
- Reviewed at: metadata tip 676c6b60a8431ead4a3820a5830843526ecffdab (tree c34f55d1c62d7c9686175739ecc0d1e6621c9986)
- Reviewer: independent agent, read-only
- First attempt: FAIL at identity gate (original mandate self-contradictory: demanded HEAD == candidate while the handoff-pin commit necessarily sits on top). Resumed under corrected protocol; substantive review completed.

## Verified identity line

```
(1) HEAD                                 = 676c6b60a8431ead4a3820a5830843526ecffdab   == declared metadata tip        OK
(2) 256c91046a00c6c18a43facf1f6ff73b576f3b23^{tree}
                                         = 576a3d683ee78cbacba427a6055a2a925a2d8725   == declared candidate tree      OK
(3) git status --porcelain               = empty                                                                      OK
(4) git diff --name-only 256c9104 HEAD   = .hermes/handoffs/P10-r16q2-superseding.json
                                           .hermes/handoffs/P10.json                  (non-handoffs count = 0)        OK
```

All four corrected checks pass. Implementation bytes reviewed from the working tree, which check 4 independently confirms are byte-identical to C3 for every path outside `.hermes/handoffs/` (also re-derived directly: `git diff --name-only 256c9104 HEAD -- .hermes/evidence/` = 0 paths, and the `SHA256SUMS` blob is `3d2ba9d5…` in both trees).

## Findings

**BLOCKER — none.**

**MINOR — `internal/agent/localstate/journal.go:495-497` (with `:348-374`): a legacy payload-less deletion row that is never exactly redelivered strands in APPLYING forever.**
The recovery filter `(!isDeletionCommandKind(j.Kind) || j.PayloadPresent)` excludes a `desired`/`forward_delete` row whose journal predates payload persistence, and `:348-374` upgrades it only on an exact redelivery of the original message ID. If the Controller re-issues with a fresh operation ID instead of redelivering the same one, that C row is never NACKed, no result is ever delivered for it, and its journal/outbox/inbox rows are never GC'd. **What it violates: nothing in the written spec.** `docs/state-model.md:85-91` (§3.2) requires exact single-step transitions and no side-effect replay — both honored; the row is deliberately held in APPLYING because the side-effect progress is unknowable, which is the fail-closed choice. Scope is bounded to pre-payload-persistence journals, and the deletion itself still converges via the re-issued command because `ApplyDesiredWithGuardAndRollback` re-installs every fence independently of the receive path (`internal/agent/reconcile/desired.go:189-201`). Reported for durability-tracking, not as a spec breach.

**NOTE — `internal/agent/localstate/journal.go:360` / `:526`: magic-string coupling.** `wasRecoveredApplying` string-matches the recovery NACK reason literal `"recovered APPLYING operation after restart"` defined 166 lines away in the same file. Renaming one site silently disables the NACKED-duplicate convergence trigger. No spec impact; pinned by `TestReceiveCommandWithPayloadUpgradesLegacyRecoveredDuplicate` (`internal/agent/localstate/journal_payload_test.go:185`).

**NOTE — `internal/agent/localstate/journal.go:694-696`: re-record suppression is payload-blind.** An existing outbox row in any phase causes `recordResultAndQueue` to return nil without comparing payloads, so a second, different result for the same operation ID would be dropped in favor of the already-queued one. Consistent with `docs/state-model.md:87-88` (§3.2: "the new session re-signs the same semantic result") and with the documented Controller re-issue rule; the durable row is never mutated in place, so the fail-closed property holds. Pre-existing code, outside the C3 delta.

**NOTE — unfenced read helpers are correct as-is.** `CurrentSession` (`journal.go:142`), `OperationPhase` (`:642`), `OutboxState` (`:849`), `OutboxContains` (`:865`), `ReceiptExists` (`:897`) do not fence. Fencing is a mutation requirement, and these are inspection reads; the fenced read path `ResultForOperation` (`:876-881`) does fence. Verified mutation surface exhaustively: 17 `db.Update` closures in `journal.go` and 17 `checkSession` call sites — every mutating entry point fences, with `receiveCommandTx` (`:318`) covering both `ReceiveCommand` entry points, and `AdvanceSession` (`:156`) being the fence-establishing call itself.

**NOTE (verification) — the spec was not edited to fit the implementation.** The repair delta (`8e39876d` → `256c9104`) touches zero paths under `docs/` or `test/contracts/`. All 136 artifacts in `test/contracts/manifest.json` verify against their recorded SHA256, including all 41 `state-model` fixture entries; the 16 inbox/outbox fixtures pin exactly the single-step and illegal transitions named at `docs/state-model.md:76-77,90-91` (`outbox-sent-gc-rejected`, `outbox-pending-sent-rejected`, `outbox-semantic-acked-gc-rejected`, `inbox-received-applied-rejected`, `inbox-applied-nacked-rejected`).

**NOTE (verification) — FSM conformance.** Outbox single-step enforced at `journal.go:728,744,761,787`; GC only after a durable receipt bound to a SEMANTIC_ACKED row (`:782-789`), and a semantic ACK alone never GCs. Inbox single-step enforced at `:438-439,456-457,554-555,577-578,598-599`; intent persists before any external side effect (`:427-443`). Delete/decommission/rotation/restore all route through this.

**NOTE (verification) — C vs D separation holds, and no D-keyed result is fabricated.** The generic identity C is the journal/outbox key (`session.go:589` passes `msgID, msgID`); recovery's NACK is recorded under C only (`journal.go:530-536`), and recovery re-asserts the *fence* (`:515-524`) rather than a result. D-keyed results (`DeleteForwardResult`) originate solely from `ReconcileOnce` after a real stop-hook outcome (`reconcile/reconciler.go:65-86` → `operation.go:18-19`), and the completed-tombstone idempotent branch reports `deleted=true` only when the pending row is already gone, which `CompleteForwardDeleteIntent` (`forward_delete.go:209-234`) permits only after the matching tombstone exists. Pinned negatively by `TestRecoverApplyingDeletionWaitsForCleanupAndKeepsFence` (`journal_payload_test.go:384-389`: "recovery fabricated a D-keyed delete result").

**NOTE (verification) — tombstone-before-stop and no-resurrection fencing hold.** `CommitDesired` writes the tombstone and removes applied state in one transaction (`applied.go:546-571`); stop hooks run only afterward (`reconcile/desired.go:348-368`). `CommitDesired` fails closed against both pending and tombstoned forwards for `ApplyApplied` (`applied.go:492-498`, `:512-516`), and the reconcile PRESENT branch rejects them before any side effect (`desired.go:291-301`). Fences are installed at receive time (`journal.go:363-370,397-404`) *and* independently in `ApplyDesired` (`desired.go:189-201`), so no snapshot path reaches the tombstone commit unfenced. Tombstone GC is refused before the durable receipt for `DeletionOperationID` (`tombstone.go:96-117`).

**NOTE (verification) — `PayloadPresent` / `RecoveredDeletionPending` are spec-consistent durable state.** Both are first-class fields on the durable journal row (`journal.go:129-138`) with defined semantics, not incidental caches. `PayloadPresent` preserves the zero-byte-payload distinction that a nil check would lose (`:246-252`). Legacy backfill is derived only from the Controller's own redelivered bytes, admitted solely by a prior hash match against the inbox dedup row (`:332`), so no state is invented. The C1 regression — non-deletion kinds losing immediate recovery — is fixed (`:495-497` restores the immediate NACK) and pinned for `probe_arm`, `probe_outcome`, `node_decommission` (`p10_r9_repair_test.go:40-70`). Transient convergence failure keeps `RecoveredDeletionPending` durable because `CompleteRecoveredDeletion` (`:410-425`) runs only after `OnRecoveredDeletion` returns nil, and `convergeRecoveredDeletions` requires `ApplyStatusFull` (`app.go:727-729`).

**NOTE (verification) — ownership boundaries respected.** Zero controller-owned paths in the repair delta (`git diff --name-only 8e39876d 256c9104 | grep -c 'internal/controller|api/|cmd/'` = 0). P10's explicit non-goal "No … force-node-delete" (`.hermes/plans/v1-beta/10-…md`) is honored: the only `node_decommission` occurrence in the delta is a test-file kind list (`p10_r9_repair_test.go:41`), with **zero** occurrences in non-test source, and no force-delete/decommission semantics were implemented — the P14-owned files `internal/agent/reconcile/{delete,decommission,recovery}.go` do not exist. All four changed source files are P10-owned per the plan's "Owned files" block (`internal/agent/app.go`, `internal/agent/control/session.go`, `internal/agent/localstate/journal.go`, `test/e2e/harness/**`).

**NOTE (verification) — evidence/handoff truthfulness confirmed by measurement, not by trusting the prose.** `sha256sum -c .hermes/evidence/p10/r16q2-superseding/SHA256SUMS` from repo root: all 17 entries OK, exit 0. The handoff's `manifest_sha256` (`P10-r16q2-superseding.json:130`) matches the actual `SHA256SUMS` bytes (`eb70e571…`), and all 15 per-gate `evidence_sha256` values match the tracked files. Gates 13/14 are recorded SKIPPED at exit 125 with reasons verified true on this host: `govulncheck` is not installed, and `test/integration` is absent. Evidence is 16 tracked `.txt` files and zero `.log` files, correctly avoiding `.gitignore:15` (`*.log`). No `OPEN_FROM_VANTAGE` or public-WAN claim appears anywhere in the evidence directory; `.hermes/handoffs/P10-r16q2-superseding.json:72,279,282` explicitly disclaim independence, and `test/e2e/walking_skeleton_test.go:132-139` asserts `NOT_TESTED` rather than fabricating vantage evidence. The post-delete oracle is exact tcp4 bindability (`test/e2e/harness/harness.go:289-303`), with the released tuple held reserved across the restart so parallel port reuse cannot impersonate the deleted Forward (`walking_skeleton_test.go:252-264`). `GOWORK=off go build ./...` exits 0.

## VERDICT: PASS

Zero BLOCKER findings against the unchanged candidate tree. The single MINOR finding is a liveness/durability-tracking gap confined to pre-payload-persistence legacy journals that are never exactly redelivered; it does not violate any written rule in `docs/state-model.md` and crosses no ownership boundary. All specification-scope items (1-6) are satisfied on the reviewed bytes.
